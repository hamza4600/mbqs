// used for $ main Page Types 
const ListViewPage = (props) => {
    const {type} = props
    return (
        <>
            <h1>
                List view Page of {type} 
            </h1>
        </>
    )
}

export default ListViewPage;